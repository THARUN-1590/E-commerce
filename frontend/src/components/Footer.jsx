import React from "react";

const Footer = () => (
  <footer className="py-3 bg-light mt-auto">
    <div className="container text-center">
      <small>© {new Date().getFullYear()} E-Mart</small>
    </div>
  </footer>
);

export default Footer;
